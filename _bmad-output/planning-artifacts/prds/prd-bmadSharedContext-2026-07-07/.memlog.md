---
topic: Shared Context System for Domain Knowledge and AI Agent Skills
updated: 2026-07-07T14:03
---

- (decision) Fast path selected. User wants industry-standard approach; lean on existing formats, embellish only where necessary.
- (decision) Standards to use: JSON-LD + schema.org (primary knowledge format), SKOS (keyword/concept taxonomy), Dublin Core (document front-matter metadata), YAML front-matter in markdown as native authoring format.
- (decision) Keyword extraction: KeyBERT or YAKE via GitHub Actions custom Python step (no dedicated Action exists; this is genuine whitespace).
- (decision) Database: Neo4j Community Edition selected (best fit for keyword/entity graph with REST+Bolt API; Cypher queries; free).
- (assumption) [ASSUMPTION] Google Open Knowledge Format = schema.org/JSON-LD. User to confirm.
- (assumption) [ASSUMPTION] GitHub as platform is fixed; no migration to other VCS.
- (assumption) [ASSUMPTION] Go API and React UI are future phases; PRD covers pilot (Phase 1) with stub API only.
- (assumption) [ASSUMPTION] Pilot domain is User Authentication only; other domains expand in later phases.
- (change) User clarified open knowledge approach is a loose interpretation guided by directory listing and markdown files.
- (change) User prefers Go from day one and expects services to run in containers.
- (change) User confirmed conflict detection should block PR merges.
- (change) User named Mindcraft as current development owner for the pilot domain.
- (decision) Pilot execution posture updated to local-first container deployment for demoability, while preserving GitHub integration patterns for later rollout.
- (decision) Converted remaining pilot unknowns to explicit non-blocking assumptions; retained only rollout-focused open questions.
- (event) PRD finalized
